# Installation & Setup

Here is how you can install our logging package `athina-logger`

Follow these steps.

1. Run `pip install athina-logger`

Next, start [logging your data](logging/log_inference_call/openai_chat).
